kubeProxyReplacement: true

ipam:
  operator:
    clusterPoolIPv4PodCIDRList: "10.42.0.0/16"
  mode: kubernetes

k8sServiceHost: localhost
k8sServicePort: 7445

l7Proxy: true

cgroup:
  autoMount:
    enabled: false
  hostRoot: /sys/fs/cgroup

securityContext:
  capabilities:
    ciliumAgent:
      - CHOWN
      - KILL
      - NET_ADMIN
      - NET_RAW
      - IPC_LOCK
      - SYS_ADMIN
      - SYS_RESOURCE
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    cleanCiliumState:
      - NET_ADMIN
      - SYS_ADMIN
      - SYS_RESOURCE

loadBalancer:
  enabled: true

operator:
  replicas: 1

gatewayAPI:
  enabled: true
  enableAlpn: true
  enableAppProtocol: true

ingressController:
  enabled: false
  default: false
  loadbalancerMode: shared

authentication:
  enabled: true
  mutual:
    # SPIRE-Verbindungstimeout von default 30s auf 60s erhöhen
    spire:
      enabled: true
      connectionTimeout: "60s"
      install:
        enabled: true
        existingNamespace: true
        server:
          dataStorage:
            enabled: false
          # --- FIX: Ressourcen explizit vorgeben ---
          # Behebt Kyverno 'require-cpu-memory-requests' und verhindert,
          # dass der Container mangels CPU-Throttle zu lange zum Booten braucht.
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi

l2announcements:
  enabled: true

hubble:
  enabled: true

  relay:
    enabled: true

  ui:
    enabled: true

  metrics:
    enabled:
      - dns
      - drop
      - tcp
      - flow
      - port-distribution
      - icmp
      - http

prometheus:
  metricsService: true
  enabled: true
  port: 9962
  serviceMonitor:
    enabled: true
    labels:
      release: kube-prometheus-stack

envoy:
  log:
    path: /dev/stdout
    defaultLevel: info
    accessLogBufferSize: 65536
