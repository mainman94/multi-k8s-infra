# Cloudflare creds sourced from OpenBao (homelab/prod/cloudflare) via the
# cluster-wide ClusterSecretStore "openbao". Each ExternalSecret targets the
# consuming app's namespace, replacing the equivalent Infisical-managed secret.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudflare-api-token
  namespace: cilium
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: cloudflare-api-token
    creationPolicy: Owner
  data:
    - secretKey: CF_DNS_API_TOKEN
      remoteRef:
        key: prod/cloudflare
        property: API_KEY
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudflare-api-token
  namespace: external-dns
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: cloudflare-api-token
    creationPolicy: Owner
  data:
    - secretKey: CF_API_TOKEN
      remoteRef:
        key: prod/cloudflare
        property: API_KEY
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudflare-cache-purge
  namespace: argocd
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: cloudflare-cache-purge
    creationPolicy: Owner
  data:
    - secretKey: zone-id
      remoteRef:
        key: prod/cloudflare
        property: ZONE_ID_HAUPTMANN_DEV
    - secretKey: api-token
      remoteRef:
        key: prod/cloudflare
        property: API_KEY
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudflare-tunnel-secret
  namespace: cloudflared
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: cloudflare-tunnel-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        credentials.json: |-
          {
            "AccountTag": "{{ .account_id }}",
            "TunnelID": "{{ .tunnel_id }}",
            "TunnelSecret": "{{ .tunnel_secret }}"
          }
        cert.pem: "{{ .cert }}"
  data:
    - secretKey: account_id
      remoteRef:
        key: prod/cloudflare
        property: ACCOUNT_ID
    - secretKey: tunnel_id
      remoteRef:
        key: prod/cloudflare
        property: TUNNEL_EGGENBERG_ID
    - secretKey: tunnel_secret
      remoteRef:
        key: prod/cloudflare
        property: TUNNEL_EGGENBERG_SECRET
    - secretKey: cert
      remoteRef:
        key: prod/cloudflare
        property: TUNNEL_EGGENBERG_CERTIFICATE
