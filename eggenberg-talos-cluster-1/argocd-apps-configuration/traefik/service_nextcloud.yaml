apiVersion: v1
kind: Service
metadata:
  name: nextcloud-external
  namespace: traefik
  labels:
    app.kubernetes.io/name: nextcloud
spec:
  ports:
    - port: 30056
      targetPort: 30056
      protocol: TCP
      name: http
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: nextcloud-external-1
  namespace: traefik
  labels:
    app.kubernetes.io/name: nextcloud
    kubernetes.io/service-name: nextcloud-external
    endpointslice.kubernetes.io/managed-by: argocd
addressType: IPv4
ports:
  - name: http
    protocol: TCP
    port: 30056
endpoints:
  - addresses:
      - 192.168.0.129
    conditions: {}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nextcloud-route
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`nc.hauptmann.dev`)
      middlewares:
        - name: default-secure-chain
      kind: Rule
      services:
        - name: nextcloud-external
          port: 30056
          passHostHeader: true
  tls:
    store:
      name: default
