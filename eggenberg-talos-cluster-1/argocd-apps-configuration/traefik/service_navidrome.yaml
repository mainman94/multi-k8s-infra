apiVersion: v1
kind: Service
metadata:
  name: navidrome-external
  namespace: traefik
  labels:
    app.kubernetes.io/name: navidrome
spec:
  ports:
    - port: 30043
      targetPort: 30043
      protocol: TCP
      name: http
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: navidrome-external-1
  namespace: traefik
  labels:
    app.kubernetes.io/name: navidrome
    kubernetes.io/service-name: navidrome-external
    endpointslice.kubernetes.io/managed-by: argocd
addressType: IPv4
ports:
  - name: http
    protocol: TCP
    port: 30043
endpoints:
  - addresses:
      - 192.168.0.129
    conditions: {}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: navidrome-route
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`navidrome.hauptmann.dev`)
      middlewares:
        - name: default-secure-chain
      kind: Rule
      services:
        - name: navidrome-external
          port: 30043
          passHostHeader: true
  tls:
    store:
      name: default
