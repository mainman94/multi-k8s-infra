# Single-instance Postgres for the contact backend.
# Namespace + common labels are applied by the including overlay (dev/test/production).
# contact-api auto-creates the `contact_requests` table on startup
# (CREATE TABLE IF NOT EXISTS), so no separate migration job is needed.
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: contact-postgres
  labels:
    app: contact-postgres
spec:
  serviceName: contact-postgres
  replicas: 1
  selector:
    matchLabels:
      app: contact-postgres
  template:
    metadata:
      labels:
        app: contact-postgres
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: postgres
          # Digest-pinned for immutability; tag kept for readability (Renovate-bumpable).
          image: docker.io/postgres:17-alpine@sha256:dc17045ccfd343b49600570ea734b9c4991cf1c3f3302e67df51e3b402dd55c4
          ports:
            - containerPort: 5432
              name: postgres
              protocol: TCP
          env:
            - name: POSTGRES_USER
              value: contact
            - name: POSTGRES_DB
              value: contact
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: contact-backend-secrets
                  key: POSTGRES_PASSWORD
            # Keep PGDATA in a subdirectory so the volume root (lost+found etc.)
            # does not collide with initdb.
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: run
              mountPath: /var/run/postgresql
            - name: tmp
              mountPath: /tmp
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "contact", "-d", "contact"]
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "contact", "-d", "contact"]
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
        - name: postgres-exporter
          # Digest-pinned for immutability; tag kept for readability (Renovate-bumpable).
          image: docker.io/prometheuscommunity/postgres-exporter:v0.15.0@sha256:386b12d19eab2a37d7cd8ca8b4c7491cc7a830d9581f49af6c98a393da9605e6
          ports:
            - containerPort: 9187
              name: metrics
              protocol: TCP
          env:
            # Built from separate vars (not the shared DATABASE_URL) so we can
            # force sslmode=disable: the pq driver defaults to requiring SSL,
            # but this Postgres has no SSL configured.
            - name: DATA_SOURCE_USER
              value: contact
            - name: DATA_SOURCE_PASS
              valueFrom:
                secretKeyRef:
                  name: contact-backend-secrets
                  key: POSTGRES_PASSWORD
            - name: DATA_SOURCE_URI
              value: "contact-postgres:5432/contact?sslmode=disable"
          readinessProbe:
            httpGet:
              path: /metrics
              port: metrics
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: "10m"
              memory: "32Mi"
            limits:
              cpu: "100m"
              memory: "64Mi"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
      volumes:
        - name: run
          emptyDir: {}
        - name: tmp
          emptyDir: {}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: longhorn
        resources:
          requests:
            storage: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: contact-postgres
  labels:
    app: contact-postgres
spec:
  clusterIP: None
  selector:
    app: contact-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
    - name: metrics
      port: 9187
      targetPort: metrics
      protocol: TCP
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: contact-postgres
  labels:
    app: contact-postgres
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: contact-postgres
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
