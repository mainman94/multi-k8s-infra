# SMTP creds sourced from OpenBao (homelab/prod/smtp) via the cluster-wide
# ClusterSecretStore "openbao". Produces the k8s Secret "contact-smtp" in the
# overlay's namespace. Replaces the SMTP_* + CONTACT_TO_EMAIL keys previously
# templated into contact-backend-secrets by the Infisical operator.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: contact-smtp
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: contact-smtp
    creationPolicy: Owner
  data:
    - secretKey: SMTP_HOST
      remoteRef:
        key: prod/smtp
        property: HOST
    - secretKey: SMTP_PORT
      remoteRef:
        key: prod/smtp
        property: PORT
    - secretKey: SMTP_USER
      remoteRef:
        key: prod/smtp
        property: USER
    - secretKey: SMTP_PASS
      remoteRef:
        key: prod/smtp
        property: PASSWORD
    - secretKey: SMTP_FROM
      remoteRef:
        key: prod/smtp
        property: FROM
    - secretKey: CONTACT_TO_EMAIL
      remoteRef:
        key: prod/smtp
        property: TO_EMAIL
