# yamllint disable rule:line-length
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: portfolio-route
spec:
  parentRefs:
    - name: public
      namespace: cilium
      sectionName: https
  hostnames:
    - hauptmann.dev
  rules:
    # Public contact form submissions go straight to contact-api. Autoscaling
    # is driven by a KEDA prometheus trigger reading Envoy request-rate
    # metrics from VictoriaMetrics (see keda.yaml), not by a routing hop.
    # Only /api/contact is exposed; /internal/* is never routed.
    - matches:
        - path:
            type: PathPrefix
            value: /api/contact
      backendRefs:
        - name: contact-api
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: portfolio-service
          port: 80
          # yamllint enable rule:line-length
