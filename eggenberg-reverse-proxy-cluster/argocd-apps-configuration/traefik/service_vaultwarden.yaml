apiVersion: v1
kind: Service
metadata:
  name: vaultwarden-external
  namespace: traefik
  labels:
    app.kubernetes.io/name: vaultwarden
spec:
  ports:
    - port: 30032
      targetPort: 30032
      protocol: TCP
      name: http
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: vaultwarden-external-1
  namespace: traefik
  labels:
    app.kubernetes.io/name: vaultwarden
    kubernetes.io/service-name: vaultwarden-external
    endpointslice.kubernetes.io/managed-by: staff
addressType: IPv4
ports:
  - name: http
    protocol: TCP
    port: 30032
endpoints:
  - addresses:
      - 192.168.0.129
    conditions: {}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: vaultwarden-route
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`vault.hauptmann.dev`)
      middlewares:
        - name: default-secure-chain
      kind: Rule
      services:
        - name: vaultwarden-external
          port: 30032
          passHostHeader: true
    - match: Host(`passwords.hauptmann.dev`)
      middlewares:
        - name: default-secure-chain
      kind: Rule
      services:
        - name: vaultwarden-external
          port: 30032
          passHostHeader: true
  tls:
    store:
      name: default
