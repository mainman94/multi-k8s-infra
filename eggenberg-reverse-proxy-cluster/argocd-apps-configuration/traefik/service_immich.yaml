apiVersion: v1
kind: Service
metadata:
  name: immich-external
  namespace: traefik
  labels:
    app.kubernetes.io/name: immich
spec:
  ports:
    - port: 30041
      targetPort: 30041
      protocol: TCP
      name: http
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: immich-external-1
  namespace: traefik
  labels:
    app.kubernetes.io/name: immich
    kubernetes.io/service-name: immich-external
    endpointslice.kubernetes.io/managed-by: argocd
addressType: IPv4
ports:
  - name: http
    protocol: TCP
    port: 30041
endpoints:
  - addresses:
      - 192.168.0.129
    conditions: {}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: immichq-route
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`immich.hauptmann.dev`)
      middlewares:
        - name: default-secure-chain
      kind: Rule
      services:
        - name: immich-external
          port: 30041
          passHostHeader: true
  tls:
    store:
      name: default
