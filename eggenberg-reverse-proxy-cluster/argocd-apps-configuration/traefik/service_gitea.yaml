apiVersion: v1
kind: Service
metadata:
  name: gitea-external
  namespace: traefik
  labels:
    app.kubernetes.io/name: gitea
spec:
  ports:
    - port: 30008
      targetPort: 30008
      protocol: TCP
      name: http
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: gitea-external-1
  namespace: traefik
  labels:
    app.kubernetes.io/name: gitea
    kubernetes.io/service-name: gitea-external
    endpointslice.kubernetes.io/managed-by: staff
addressType: IPv4
ports:
  - name: http
    protocol: TCP
    port: 30008
endpoints:
  - addresses:
      - 192.168.0.129
    conditions: {}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: gitea-route
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`git.hauptmann.dev`)
      middlewares:
        - name: default-secure-chain
      kind: Rule
      services:
        - name: gitea-external
          port: 30008
          passHostHeader: true
  tls:
    store:
      name: default
