apiVersion: v1
kind: Service
metadata:
  name: bookstack-external
  namespace: traefik
  labels:
    app.kubernetes.io/name: bookstack
spec:
  ports:
    - port: 30214
      targetPort: 30214
      protocol: TCP
      name: http
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: bookstack-external-1
  namespace: traefik
  labels:
    app.kubernetes.io/name: bookstack
    kubernetes.io/service-name: bookstack-external
    endpointslice.kubernetes.io/managed-by: staff
addressType: IPv4
ports:
  - name: http
    protocol: TCP
    port: 30214
endpoints:
  - addresses:
      - 192.168.0.129
    conditions: {}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: bookstack-route
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`bookstack.hauptmann.dev`)
      middlewares:
        - name: default-secure-chain
      kind: Rule
      services:
        - name: bookstack-external
          port: 30214
          passHostHeader: true
  tls:
    store:
      name: default
