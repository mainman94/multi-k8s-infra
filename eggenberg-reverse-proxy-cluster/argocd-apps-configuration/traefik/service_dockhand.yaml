apiVersion: v1
kind: Service
metadata:
  name: dockhand-external
  namespace: traefik
  labels:
    app.kubernetes.io/name: dockhand
spec:
  type: ExternalName
  externalName: 192.168.0.129
  ports:
    - port: 30328
      targetPort: 30328
      protocol: TCP
      name: http
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: dockhand-route
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`dockhand.hauptmann.dev`)
      middlewares:
        - name: default-secure-chain
      kind: Rule
      services:
        - name: dockhand-external
          port: 30328
          passHostHeader: true
  tls:
    store:
      name: default
