apiVersion: v1
kind: Service
metadata:
  name: jellystat-external
  namespace: traefik
  labels:
    app.kubernetes.io/name: jellystat
spec:
  ports:
    - port: 30176
      targetPort: 30176
      protocol: TCP
      name: http
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: jellystat-external-1
  namespace: traefik
  labels:
    app.kubernetes.io/name: jellystat
    kubernetes.io/service-name: jellystat-external
    endpointslice.kubernetes.io/managed-by: staff
addressType: IPv4
ports:
  - name: http
    protocol: TCP
    port: 30176
endpoints:
  - addresses:
      - 192.168.0.129
    conditions: {}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jellystat-route
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`jellystat.hauptmann.dev`)
      middlewares:
        - name: default-secure-chain
      kind: Rule
      services:
        - name: jellystat-external
          port: 30176
          passHostHeader: true
  tls:
    store:
      name: default
