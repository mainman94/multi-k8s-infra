# ServiceEntry for Authentik
apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: authentik-service-entry
spec:
  hosts:
    - auth.philippmhauptmann.me
  addresses:
    - 192.168.0.129
  ports:
    - number: 80
      name: my-http-port
      protocol: HTTP
  location: MESH_EXTERNAL
  resolution: STATIC
  endpoints:
    - address: 192.168.0.129
      ports:
        my-http-port: 30140
---
# VirtualService for Authentik
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: authentik-virtual-service
spec:
  hosts:
    - "auth.philippmhauptmann.me"
  gateways:
    - unified-ingress-gateway
  http:
    - route:
        - destination:
            host: auth.philippmhauptmann.me
            port:
              number: 80
      headers:
        response:
          set:
            Strict-Transport-Security: "max-age=31536000; includeSubDomains"
            X-Frame-Options: "DENY"
            X-Content-Type-Options: "nosniff"
            X-XSS-Protection: "1; mode=block"
